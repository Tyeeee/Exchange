package com.hynet.heebit.components.widget.actionsheet;import android.graphics.drawable.Drawable;import android.view.MenuItem;import android.view.View;import com.hynet.heebit.components.widget.actionsheet.listener.OnSheetItemListener;import androidx.annotation.ColorInt;import androidx.annotation.DrawableRes;import androidx.core.graphics.drawable.DrawableCompat;public class ActionSheetMenuItem implements OnSheetItemListener {    @ColorInt    private int textColor;    @ColorInt    private int tintColor;    private Drawable icon;    private String title;    private int id;    private MenuItem menuItem;    @DrawableRes    private int background;    public ActionSheetMenuItem(MenuItem menuItem, @ColorInt int textColor, @DrawableRes int background, @ColorInt int tintColor) {        this.menuItem = menuItem;        this.icon = menuItem.getIcon();        this.id = menuItem.getItemId();        this.title = menuItem.getTitle().toString();        this.textColor = textColor;        this.background = background;        this.tintColor = tintColor;        // Check if we have a tint to be applied to an icon        if (this.tintColor != View.NO_ID && icon != null) {            icon = DrawableCompat.wrap(icon);            DrawableCompat.setTint(icon, this.tintColor);        }    }    public Drawable getIcon() {        return icon;    }    public MenuItem getMenuItem() {        return menuItem;    }    @DrawableRes    public int getBackground() {        return background;    }    public int getId() {        return id;    }    @ColorInt    public int getTextColor() {        return textColor;    }    @Override    public String getTitle() {        return title;    }}